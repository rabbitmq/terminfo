defmodule Terminfo.MixProject do
  use Mix.Project

  def project do
    [
      app: :terminfo,
      description: "Helpers to query a terminal's capatilities",
      version: "0.1.0",
      language: :erlang,
      deps: []
    ]
  end
end
